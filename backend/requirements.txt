# ── Web server ────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Audio I/O & analysis ──────────────────────────────────────────────────────
librosa
soundfile
numpy
pydub

# ── Deep learning — stem separation ──────────────────────────────────────────
# Install PyTorch separately from https://pytorch.org/get-started/locally/
# to pick the right CUDA/CPU build for your machine. Example (CPU-only):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch
torchaudio
demucs

# ── Note: ffmpeg is a system dependency, not a pip package ───────────────────
# Install via: winget install ffmpeg   OR   choco install ffmpeg